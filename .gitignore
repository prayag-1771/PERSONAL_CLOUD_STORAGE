# Build artifacts
build/
storage/

# Binaries (anchored, so they do not also match src/client and src/server)
/client
/server
/pcs-client
/pcs-server
*.o
*.a

# Metadata / runtime files
*.ecmeta
*.bin
pending/
*.manifest

# Test / scratch files
a.txt
b.txt
c.txt
secret.txt
test.txt

# Prototype directories
phase9/
phase10/
phase11/
data/

# TLS certs (auto-generated)
*.crt
*.key
auth.token

# OS noise
.DS_Store
Thumbs.db
